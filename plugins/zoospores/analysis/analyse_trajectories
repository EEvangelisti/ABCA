#!/usr/bin/env bash
set -Eeuo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
CONFDIR="$(realpath -- "${1:-.}")"

"$ROOT/run" extract "$CONFDIR"

export SKIP_PYTHON_SETUP=1

"$ROOT/run" analyse "$CONFDIR"
"$ROOT/run" plot "$CONFDIR"
