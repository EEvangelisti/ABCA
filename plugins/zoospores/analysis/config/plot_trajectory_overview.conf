# Metric datasets.
# At least one directory must be provided. Both may be provided.
FILTERED_METRICS_DIR=""
COMPLETE_METRICS_DIR="../../output/metrics"

# Output directory.
OVERVIEW_DIR="../../output/trajectory_overview"

# Plotting parameters.
ANGULAR_BINS=36
# MAX_TRACKS=0 displays all trajectories in the global centred-trajectory panel.
MAX_TRACKS=0
LINE_WIDTH=0.55
# MAX_TRACKS_PER_DECILE=0 displays all trajectories in each decile panel
MAX_TRACKS_PER_DECILE=500
DPI=300
