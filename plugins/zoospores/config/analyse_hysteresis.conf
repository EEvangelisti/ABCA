# Input trajectory metrics.
METRICS_DIR="../../output/metrics"

# Output directory.
HYSTERESIS_DIR="../../output/hysteresis_sensitivity"

# Symmetric hysteresis half-widths tested around the Otsu threshold.
# Values are comma-separated and expressed in the speed unit used by
# step_metrics.csv.
HYSTERESIS_WIDTHS="0,5,10,15,20,30,40"

# Number of histogram bins used to estimate the Otsu threshold.
OTSU_BINS=256

# Number of bins used in duration, length, and speed histograms.
HISTOGRAM_BINS=50

# Figure resolution.
DPI=300
